PREFIX org: <http://www.w3.org/ns/org#>
PREFIX besluit: <http://data.vlaanderen.be/ns/besluit#>

INSERT {
  GRAPH ?targetGraph {
    ?s org:classification ?o .
  }
}
WHERE {
  GRAPH ?g {
    ?s besluit:classificatie ?o .
  }
  BIND(?g AS ?targetGraph)
}
